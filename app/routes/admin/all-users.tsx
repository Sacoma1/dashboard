import Header from "components/Header";

const AllUsers = () => {
  return (
    <main className="dashboard wrapper">
      <Header
        title={`Trips page `}
        description="Check out our current users in real tiem"
      />
      All users page contents
    </main>
  );
};

export default AllUsers;
